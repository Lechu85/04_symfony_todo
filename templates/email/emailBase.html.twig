{% apply inline_css(source('@email_styles/email.css')) %}
    <!doctype html>
    <html lang="en">
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
        <meta name="viewport" content="width=device-width">
        <title>{{ email.subject }}</title>
        <style>

        </style>
    </head>
    <body>
    <div class="body">
        <div class="container">
            <div class="header text-center">
                <a href="{{ url('app_homepage') }}">
                    {# <img src="{{ absolute_url(asset('images/email/logo.png')) }}" class="logo" alt="SpaceBar Logo">  #}
                    <img src="{{ email.image('@images/email/logo.png') }}" class="logo" alt="Logo">
                </a>
            </div>

             {% block content %}{% endblock %}


            <div class="footer">
                <p class="block">
                    Cheers,<br><br>
                    Your friendly <em>Space Bar Team</em>
                </p>
                <p class="block bottom text-center">
                    Sent with ❤️ from the friendly folks at The Space Bar
                </p>
            </div>
        </div>
    </div>
    </body>
    </html>
{% endapply %}
